{
  "name": "cipherroom-server",
  "version": "1.0.0",
  "private": true,
  "description": "Room-based encrypted chat relay. The server never sees plaintext messages, usernames aside — it only relays ciphertext blobs between clients in the same room.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
